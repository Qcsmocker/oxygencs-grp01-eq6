{
  // Specifies additional paths for Python analysis (e.g., custom module directories)
  "python.analysis.extraPaths": ["./src"],

  // Exclude compiled Python cache files and directories from the file explorer
  "files.exclude": {
      "**/__pycache__": true, 
      "**/*.pyc": true         
  }
}